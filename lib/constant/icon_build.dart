import 'package:flutter/material.dart';

class IconBuild {
  final dynamic page;
  final String name;
  final Widget iconName;
  final Function onTap;

  IconBuild(
      {required this.page,
      required this.name,
      required this.iconName,
      required this.onTap});
}
